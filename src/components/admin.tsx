'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
export function AdminControls({configured}:{configured:Record<string,boolean>}){const [message,setMessage]=useState(''),[busy,setBusy]=useState(false);const router=useRouter();async function action(body:unknown){setBusy(true);setMessage('');try{const r=await fetch('/api/admin',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});if(!r.ok)throw Error((await r.json()).error);setMessage('Gespeichert.');router.refresh();}catch(e){setMessage((e as Error).message);}finally{setBusy(false);}}
 return <><form className="panel" onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);void action({action:'settings',data:Object.fromEntries(f)});e.currentTarget.reset();}}><h2>Verbindungen</h2><p className="muted">Zugangsdaten werden verschlüsselt gespeichert. Leere Felder behalten den bisherigen Wert.</p><div className="form-grid">{[['TMDB_TOKEN','TMDB Read Access Token'],['TVDB_API_KEY','TVDB API-Key'],['TVDB_PIN','TVDB PIN (falls erforderlich)'],['PLEX_URL','Plex-Server URL'],['PLEX_TOKEN','Plex-Token'],['PLEX_ACCOUNT_ID','Plex Account-ID'],['PLEX_SERVER_ID','Plex Server-UUID'],['PLEX_WEBHOOK_SECRET','Webhook-Geheimnis']].map(([key,label])=><label key={key}>{label}<span className={`connection-status ${configured[key]?'connected':''}`}>{configured[key]?'Eingerichtet':'Noch offen'}</span><input name={key} type={key.includes('TOKEN')||key.includes('KEY')||key.includes('SECRET')||key.includes('PIN')?'password':'text'} autoComplete="off" placeholder={configured[key]?'Gespeichert — zum Ändern neu eingeben':''}/></label>)}</div><button className="button primary" disabled={busy}>Verbindungen speichern</button></form><div className="panel"><h2>Metadaten ergänzen</h2><p className="muted">Erst Plex, danach TMDB für Filme und TVDB für Serien. Dein Tagebuch bleibt währenddessen benutzbar.</p><div className="button-row"><button className="button primary" disabled={busy} onClick={()=>action({action:'enrich'})}>Fehlende Metadaten laden</button><button className="button" disabled={busy} onClick={()=>action({action:'retry'})}>Fehlgeschlagene Aufgaben wiederholen</button></div></div>{message&&<p className="notice" role="status">{message}</p>}</>;
}
