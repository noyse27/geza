'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
import { ArrowUpRight } from 'lucide-react';
export function LoginForm(){const [error,setError]=useState(''),[busy,setBusy]=useState(false);const router=useRouter();return <form className="panel login-form" onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');const data=new FormData(e.currentTarget);try{const r=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:data.get('username'),password:data.get('password')})});const result=await r.json();if(!r.ok)throw Error(result.error);router.push(result.next);router.refresh();}catch(e){setError((e as Error).message||'Anmelden fehlgeschlagen.');}finally{setBusy(false);}}}><h2>Anmelden</h2><label>Benutzername<input name="username" autoComplete="username" required autoFocus/></label><label>Passwort<input name="password" type="password" autoComplete="current-password" required/></label>{error&&<p className="error" role="alert">{error}</p>}<button className="button primary" disabled={busy}>{busy?'Wird angemeldet …':'Tagebuch öffnen'}<ArrowUpRight size={18}/></button><p className="muted small">Der öffentliche Katalog ist auch ohne Anmeldung zugänglich.</p></form>;}
